# Human-Behavior-Recognition-
A simple PyTorch project that trains a ResNet-18 model on the Stanford-40 Actions dataset and runs image classification on custom photos. Includes basic training, validation, model saving, and an easy inference script for Google Colab.
